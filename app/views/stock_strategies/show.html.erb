<div class="card bg-base-100 shadow">
  <div class="card-body p-0">
    <div class="flex items-center gap-3 px-5 pt-5">
      <span class="icon-[charm--chart-line] size-4.5"></span>
      <span class="font-medium"><%= @stock_strategy.name %> - <%= @stock_strategy.latest_price %></span>
    </div>
  </div>
  <%= render partial: "stock_price_chart",
             locals: { stock_data: @stock_strategy.stock_history_data.to_json } %>
</div>